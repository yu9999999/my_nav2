这里写了一个插件，该插件写了三个类，其中两个去订阅数据，然后再进行数据处理，然后根据数据进行操作，实现行人避障和防跌落功能。还写了一个类，主要去检测障碍物距离与目标点是否被占用的情形，当障碍物距离过近需要后退操作，当目标点被占用，机器人需要暂停。
在主程序中，写了一个函数，该函数是为了判断机器人方向与到局部目标点方向之间的夹角，<=90°则为true，反正为false。准确的说就是检测局部目标点是在机器人正前方还是后方。
然后针对该情形，加入超近距离判断是否后退避障
